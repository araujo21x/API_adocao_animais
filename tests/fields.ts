const emailLogin = 'login_client@exemple.com';
const forgotPassword = 'recoverPassword@exemple.com';
const password = '10203040';

const emailONG = (count: number) => `email_ONG${count}@exemple.com`;
const emailCommon = (count: number) => `email_Common${count}@exemple.com`;

export {
  emailLogin,
  forgotPassword,
  emailONG,
  emailCommon,
  password
};
